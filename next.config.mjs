/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['localhost','nova-next-gen-server.onrender.com','deep-sky-server.onrender.com','res.cloudinary.com'],
  }, 
};

export default nextConfig;
